export interface Details {
    ticket: {
        id: string;
        ticketType: string;
    },
    name: string;
    email: string;
    numerOfTickets: number;
    dataUrl: string;
    request: string;
}